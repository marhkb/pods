using Gtk 4.0;

template $PdsCircularProgressBar: Widget {
  halign: center;
  valign: center;

  Image image {
    notify::icon-name => $on_image_notify_icon_name() swapped;

    styles [
      "dim-icon",
    ]
  }
}
