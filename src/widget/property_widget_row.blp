using Gtk 4.0;
using Adw 1;

template $PdsPropertyWidgetRow: Adw.ActionRow {
  notify::title => $on_notify_title() swapped;
  activatable: false;

  Adw.Bin bin {
    notify::child => $on_bin_notify_child() swapped;
  }
}
